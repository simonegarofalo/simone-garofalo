---
const { title, description, image, id } = Astro.props;
const bgStyle = `background-image: url('${image}')`;
---

<div
  id={id}
  class="h-[600px] col-span-12 lg:col-span-6 lg:row-span-2 flex items-center justify-center bg-cover bg-center bg-no-repeat rounded-xl transition duration-700 hover:brightness-75 cursor-pointer"
  style={bgStyle}
>
  <div class="text-center text-white">
    <h3 class="text-6xl md:text-7xl leading-relaxed">{title}</h3>
    <p>{description}</p>
  </div>
</div>
<!-- <div class="overflow-hidden cursor-pointer">
  <div
    id={id}
    class="flex items-center justify-center h-lvh w-screen bg-cover bg-center bg-no-repeat transform transition-all duration-1000 hover:scale-110"
    style={bgStyle}
  >
    <div class="text-center text-white">
      <h3 class="text-6xl md:text-7xl leading-relaxed">{title}</h3>
      <p>{description}</p>
    </div>
  </div>
</div> -->
